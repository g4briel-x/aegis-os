# Project metadata commands

```console
aegis version
aegis --repo-root . info
aegis --repo-root . status
```

`version` does not require a repository. `info` reports Python entrypoints and
important repository paths. `status` reports loaded registries, assets and
configuration files.
