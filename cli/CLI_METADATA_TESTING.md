# Metadata command testing

`tests/runtime/test_cli.py` verifies `version`, `info`, `status`, documentation
and release catalog routing through the Python parser.

```console
python -m pytest tests/runtime/test_cli.py
```
