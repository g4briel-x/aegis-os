# Command implementation

Command parsing and dispatch live in
`runtime/src/aegis_runtime/cli.py`. Domain behavior is separated into runtime
modules for configuration, registries, validation, reports, generation and
execution.

This directory is intentionally documentation-only after the Python-only
migration.
