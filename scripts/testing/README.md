# Runtime testing

The test suite is implemented with pytest under `tests/runtime/`.

```console
python -m pytest tests/runtime
```

Tests cover registry loading, resolution, validation, reporting, generators,
execution contracts, contexts, sessions, orchestration, lifecycle and audits.
